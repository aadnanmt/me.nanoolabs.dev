---
import { getMood } from "@lib/imood"
import { getEmoji } from "@lib/emoji"

const { mood, content, updated, error } = await getMood()

// Silent fail
if (error || !mood) {
  return null
}

const emoji = getEmoji(mood)
const updatedText = updated
  ? ` - Updated ${new Date(updated).toLocaleDateString("en-US", {
      month: "short",
      day: "numeric",
    })}`
  : ""
---

<div
  class="flex items-center gap-4 border border-black/10 p-4 dark:border-white/10"
>
  <div
    class="flex h-12 w-12 flex-shrink-0 items-center justify-center bg-black/5 text-2xl dark:bg-white/5"
  >
    {emoji}
  </div>
  <div class="flex flex-col gap-0.5 overflow-hidden">
    <span class="text-xs font-semibold tracking-widest uppercase opacity-50">
      Current mood{updatedText}
    </span>
    <span class="truncate text-sm font-semibold text-black dark:text-white">
      {mood}
    </span>
    {content && <span class="text-xs break-words opacity-60">{content}</span>}
  </div>
</div>
