---
import { SITE, SOCIALS } from "@consts"
import Link from "@components/Link.astro"
import { IconSend } from "@lib/iconSystem"
---

<section class="animate space-y-4">
  <h5 class="flex items-center gap-2 font-semibold text-black dark:text-white">
    <span class="size-5 fill-zinc-500"><Fragment set:html={IconSend} /></span>
    Let's Connect
  </h5>
  <article>
    <p>
      Have a wild idea, want to collaborate, or just fancy a technical
      discussion? Reach out anytime. Let's build something that actually scales.
    </p>
  </article>
  <ul class="flex flex-wrap gap-2">
    {
      SOCIALS.map((SOCIAL) => (
        <li class="flex gap-x-2 text-nowrap">
          <Link
            href={SOCIAL.HREF}
            external
            aria-label={`${SITE.NAME} on ${SOCIAL.NAME}`}
          >
            {SOCIAL.NAME}
          </Link>
          {"/"}
        </li>
      ))
    }
    <li class="line-clamp-1">
      <Link href={`mailto:${SITE.EMAIL}`} aria-label={`Email ${SITE.NAME}`}>
        {SITE.EMAIL}
      </Link>
    </li>
  </ul>
</section>
